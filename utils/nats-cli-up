#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"

# shellcheck disable=SC1090
source "$ROOT_DIR/compose/load-secrets.sh" >/dev/null

docker compose -p fscl -f "$ROOT_DIR/compose/process-stack.yaml" --profile infra --profile tools up -d nats-cli
